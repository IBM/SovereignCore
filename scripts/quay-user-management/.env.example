# Quay Team Setup - Environment Variables
# Copy this file to .env and fill in your values

# Required: Quay registry URL
# Example: https://quay.example.com or https://registry-quay-quay-enterprise.apps.example.com
export QUAY_URL=https://your-quay-registry.example.com

# Authentication: Choose one of the following options

# Option 1: Use existing API Token (recommended for production)
# Generate this token from Quay UI: default-org organization > Applications > default-appication > Generate Token
# The token must have the following scopes: super:user, org:admin, repo:admin, repo:create, user:admin, user:read
# Note: If you use --no-revoke-token option with OAuth auto-generation,
#       the generated token will be automatically saved here for reuse
export QUAY_API_TOKEN=your-superuser-api-token-here

# Option 2: Auto-generate OAuth Token (requires Hub Cluster access)
# If QUAY_API_TOKEN is not set, the script will automatically generate OAuth tokens
# using the following credentials. This requires:
# - Access to Hub Cluster (must be logged in via 'oc login')
# - Superuser credentials for Quay
# - OAuth credentials secret in Hub Cluster (quay-oauth-credentials)
# export QUAY_SUPER_USER=your-superuser-username
# export QUAY_SUPER_PASSWORD=your-superuser-password
# export QUAY_NAMESPACE=quay-enterprise  # Optional: Hub Cluster namespace (default: quay-enterprise)

# Required: User to add to teams
# This user will be added to the "sovereign-core-read" team in each organization
# If the user does not exist, it will be created automatically with an auto-generated password
export TEAM_USER_NAME=sovereign-core-read

# Optional: Additional curl options
# Useful for SSL certificate issues, timeouts, or proxy settings
# Examples:
#   - Skip SSL certificate verification (use only in dev/test environments):
#     export CURL_OPTS="--insecure"
#   - Set connection timeout (in seconds):
#     export CURL_OPTS="--connect-timeout 30"
#   - Set maximum time for the entire operation (in seconds):
#     export CURL_OPTS="--max-time 300"
#   - Combine multiple options:
#     export CURL_OPTS="--insecure --connect-timeout 30 --max-time 300"
# export CURL_OPTS=""
